{
  "name": "zoomlike",
  "version": "0.1.0",
  "description": "A Zoom-like meeting app with a real-time collaborative whiteboard, fully hosted on Cloudflare (free tier).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test:e2e": "node tests/e2e.mjs",
    "test:controls": "node tests/e2e-controls.mjs",
    "test:auth": "node tests/e2e-auth.mjs",
    "test": "npm run test:e2e && npm run test:controls && npm run test:meeting && npm run test:auth",
    "test:meeting": "node tests/e2e-meeting.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "wrangler": "^3.90.0"
  }
}
